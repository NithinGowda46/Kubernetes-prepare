apiVersion: batch/v1
kind: Job
metadata:
  name: nginx-job
  namespace: nginx
spec:
  template:
    spec:
      containers:
      - name: backup
        image: busybox
        command: ["echo", "Hello from the Nginx backup job!"]
      restartPolicy: Never